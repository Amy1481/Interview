Static keyword
It is of 2 types:
1. Static variables-These are of 2 types:
   a) In Function-Variables declared as static in a function are created & initialized once for the lifetime of the program.
   b) In Class-Static variables in a class are created & initialized once.They are shared by all the objects of the class.

2.Static objects