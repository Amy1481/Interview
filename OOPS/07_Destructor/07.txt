Destructor works opposite of constructor i.e deallocating the memory i.e deleting the memory that the object had been assigned.It is a special function and automatically compiler does it for us i.e self-destructive when we don't create one.As our compiler automatically cleans up the objects after running them.It only allocates statically allocated memory.Thus the same problem arrives here as we have to deallocate the memory dynamically as we have dynamically allocate the memory.To dynamically allocate the memory we use new keyword so to deallocate we have to use the keyword delete.To free the memory (as it is pointers concept not oops):let we have a memory address 100 inside heap and we have a ptr pointing to the memory address 100 so to free the memory we write deletePtr.

deletePtr doesn't mean deleting the ptr,it means deleting the memory pointer ptr is pointing to.